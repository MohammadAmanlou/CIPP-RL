# Processed professor data

`CIPP-D.csv` and `CIPP-R.csv` are faithful row-wise exports of the `Rewards` and
`ID` sheets in the supplied legacy XLS workbooks.  Row 0 is the dummy `Rest`
entry.  The raw XLS files are retained in `data/` for provenance.
